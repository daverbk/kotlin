spring:
  application:
    name: spring
  datasource:
    driver-class-name: org.h2.Driver
    url: jdbc:h2:file:./data/testdb2
    username: name
    password: password
  sql:
    init:
      schema-locations: classpath:schema.sql
      mode: always
